{
  "name": "fastifyapp",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "start:production": "tsc && NODE_ENV=production node dist/app.js",
    "start_cluster": "tsc && node dist/app.js -c",
    "start_cluster:production": "tsc && NODE_ENV=production node dist/app.js -c",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "dependencies": {
    "@fastify/view": "^10.0.1",
    "fastify": "^5.0.0",
    "handlebars": "^4.7.8",
    "kysely": "^0.27.4",
    "mysql2": "3.11.3"
  }
}
